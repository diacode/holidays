%header#main_header
  = image_tag 'logo-white.png', width: 125, class: 'logo'
  %nav.nav
    %ul
      %li
        %a.btn{href: '#'} Request holidays
      %li
        %a{href: '#'}
          = image_tag gravatar_url(current_user.email), class: 'avatar'
        %ul.dropdown
          %li
            = link_to destroy_user_session_path, method: :delete do
              = fa_icon 'sign-out'
              Sign out

